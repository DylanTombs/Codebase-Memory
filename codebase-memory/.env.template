GITHUB_TOKEN=
OPENAI_API_KEY=
REPO_NAME=owner/repo

# Set to true to skip OpenAI entirely (uses local sentence-transformers + scripted answer)
DEMO_MODE=false

# The scripted answer shown in DEMO_MODE — tailor this to your demo question
DEMO_ANSWER=We switched from REST to gRPC in PR #34 after profiling revealed that serialisation overhead was the primary bottleneck in our internal service calls. The team evaluated gRPC's binary protocol (protobuf) against REST+JSON and found a 3× reduction in payload size and significantly lower latency under load. PR #41 followed up with the shared proto definitions and client codegen pipeline.
